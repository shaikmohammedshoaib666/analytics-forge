# Core — installs from wheels on Streamlit Cloud / typical college PCs
# (no gurobi / torch / pyspark)
streamlit>=1.28.0,<1.46.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.3.0
plotly>=5.18.0
matplotlib>=3.7.0
seaborn>=0.13.0
scikit-learn>=1.3.0,<2.0.0
openai>=1.0.0
google-genai>=1.0.0
python-dotenv>=1.0.0
openpyxl>=3.1.0
pyarrow>=14.0.0
duckdb>=0.9.0
PyYAML>=6.0

# Separate libraries for catalog models — shipped in MAIN so
# `pip install -r requirements.txt` (college / Streamlit Cloud) makes them run.
# Soft-fail in-app remains only as a safety net if import still fails after install.
statsmodels>=0.14.0,<0.15.0
xgboost>=2.0.0,<4.0.0
lightgbm>=4.0.0,<5.0.0
pulp>=2.8.0,<4.0.0
# Prophet: usually has wheels; Cloud build can still flake (cmdstan) — soft-fails if so.
prophet>=1.1.5,<1.4.0
